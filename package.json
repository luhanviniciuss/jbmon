{
  "name": "jbmon",
  "version": "0.1.0",
  "private": true,
  "description": "MMORPG Pokémon 2D no navegador (Phaser 3 + Socket.io + Prisma)",
  "main": "server/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch server/index.js",
    "start": "prisma db push --skip-generate && node server/index.js",
    "setup": "prisma db push",
    "studio": "prisma studio",
    "export": "node scripts/export.js",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^5.22.0",
    "socket.io": "^4.8.0"
  }
}
